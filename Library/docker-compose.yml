version: '3.8'

services:
  library:
    image: ${DOCKER_REGISTRY-}library
    build:
      context: .
      dockerfile: .
    ports:
       - "8080:80"
       - "443:443"
    depends_on: 
        - mysqldocker
  
  mysqldocker:
    image: mysql
    environment:
      - MYSQL_ROOT_PASSWORD=root
    volumes:
      - D:\Dados\volumes\mysql:/var/opt/mysql/data
    ports:
      - "3307:3306"